#include <iostream>
using namespace std;
int integerPower(float a,int b)
	{
		int result =1;
		while (b != 0) {
        result *= a;
        --b;
        
	}
		return result;
	}
int main() 
{
    int exponent;
    float base,result;
    cout << "Enter base and exponent respectively:  ";
    cin >> base >> exponent;
    cout << base << "^" << exponent << " = ";
     cout <<integerPower(base,exponent);
   
    
    return 0;
}
